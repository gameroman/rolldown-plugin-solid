{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "type": "module",
  "scripts": {
    "format": "biome check --write",
    "lint": "biome check",
    "test": "bun run --cwd='./packages/rolldown-plugin-solid' test",
    "build": "bun run --cwd='./packages/rolldown-plugin-solid' build"
  },
  "devDependencies": {
    "@gameroman/config": "0.2.9",
    "@biomejs/biome": "2.5.9",
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
